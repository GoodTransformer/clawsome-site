# TODO

These are the only true external-input gaps left before a live launch:

- Replace placeholder contact email in `.env`
- Replace placeholder booking URL in `.env`
- Replace placeholder LinkedIn URL in `.env`
- Connect a live form endpoint through `PUBLIC_CONTACT_FORM_ENDPOINT`
- Confirm the final production site URL through `PUBLIC_SITE_URL`
- Add the same live values as GitHub repository variables if GitHub Pages is the deployment target
- Add live proof assets if available
- Complete final legal review of the policy and terms copy
