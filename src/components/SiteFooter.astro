---
import Logo from "./Logo.astro";
import { withBase } from "@data/paths";
import {
  footerNavigation,
  getContactEmailHref,
  getPrimaryCtaHref,
  isPlaceholderValue,
  legalNavigation,
  siteConfig
} from "@data/site";

const primaryHref = getPrimaryCtaHref();
---

<footer class="site-footer">
  <div class="container footer-shell">
    <div class="footer-brand">
      <a href={withBase("/")} aria-label="Clawsome home">
        <Logo tone="light" />
      </a>
      <p>Practical OpenClaw and Claude builds for high-value SME workflows.</p>
    </div>

    <nav class="footer-nav" aria-label="Footer">
      {footerNavigation.map((item) => (
        <a href={withBase(item.href)}>{item.label}</a>
      ))}
    </nav>

    <div class="footer-links">
      <a
        href={primaryHref}
        target={primaryHref.startsWith("http") ? "_blank" : undefined}
        rel={primaryHref.startsWith("http") ? "noreferrer" : undefined}
      >
        Book an audit
      </a>
      {!isPlaceholderValue(siteConfig.email) && (
        <a href={getContactEmailHref()}>{siteConfig.email}</a>
      )}
    </div>
  </div>

  <div class="container footer-meta">
    <p>{siteConfig.name} • {siteConfig.location}</p>
    <div class="footer-legal">
      {legalNavigation.map((item) => (
        <a href={withBase(item.href)}>{item.label}</a>
      ))}
    </div>
  </div>
</footer>
